---
import { Image } from "astro:assets";
const {
  href = "",
  icon = "",
  label = "",
  linkClass = "",
  iconClass = "",
  labelClass = "",
} = Astro.props;

const showLabel = label.trim() !== "";
---

<a href={href} target="_blank" class={` ${linkClass}`}>
  <Image
    src={icon}
    alt={label || "social icon"}
    class={iconClass}
    height={28}
    width={28}
  />
  {showLabel && <span class={labelClass}>{label}</span>}
</a>
