---
import { Image } from "astro:assets";
import { ChevronRight } from "lucide-react";
import poster from "@/assets/images/Poster.webp";

const quickFacts = [
  "5+ years building web applications",
  "Expertise in AI/ML and Computer Vision",
  "Passionate about cloud architecture",
  "Love solving complex problems",
];
---

<div class="grid md:grid-cols-2 gap-12 items-start">
  <!-- Left: Text Content - Wrapped in secondary glass panel -->
  <div class="space-y-6 glass-panel-wrapper glass-panel-secondary">
    <div class="space-y-4">
      <p class="text-body-lg text-textSecondary">
        Hello! I'm Farhan, a full-stack software engineer based in Pakistan with a passion for 
        building exceptional digital experiences. I specialize in creating scalable web applications, 
        AI-powered solutions, and cloud infrastructure that solve real-world problems.
      </p>
      <p class="text-body-lg text-textSecondary">
        Here's a few technologies I've been working with recently:
      </p>
      <div class="flex flex-wrap gap-2">
        {["React & Next.js", "Python & FastAPI", "TypeScript", "AWS & Docker", "PostgreSQL", "Computer Vision"].map((tech) => (
          <span class="liquid-glass-badge text-textPrimary text-xs font-medium">
            {tech}
          </span>
        ))}
      </div>
    </div>

    <!-- Quick Facts - Tertiary glass panel -->
    <div class="pt-6 border-t border-black/5 glass-panel-tertiary rounded-2xl p-6 mt-6">
      <h4 class="text-lg font-bold mb-4 text-textPrimary">Quick Facts</h4>
      <ul class="space-y-2" role="list">
        {quickFacts.map((fact) => (
          <li class="flex items-center gap-2 text-sm text-textSecondary" role="listitem">
            <span class="text-accent-blue" aria-hidden="true">→</span>
            <span>{fact}</span>
          </li>
        ))}
      </ul>
    </div>
  </div>

  <!-- Right: Image/Video - Frosted glass -->
  <div class="relative">
    <div class="liquid-glass-frosted p-4 rounded-3xl liquid-glass-hover glass-shimmer transition-all">
      <div class="relative rounded-lg overflow-hidden">
        <img
          src={poster.src}
          alt="Farhan - Full-Stack Developer"
          class="w-full h-auto rounded-lg"
        />
        <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"></div>
      </div>
    </div>
  </div>
</div>
