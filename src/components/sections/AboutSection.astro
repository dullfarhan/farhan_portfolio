---
import { Image } from "astro:assets";
import { ChevronRight } from "lucide-react";
import poster from "@/assets/images/Poster.webp";

const quickFacts = [
  "5+ years building web applications",
  "Expertise in AI/ML and Computer Vision",
  "Passionate about cloud architecture",
  "Love solving complex problems",
];
---

<div class="grid md:grid-cols-2 gap-12 items-start">
  <!-- Left: Text Content -->
  <div class="space-y-6">
    <div class="space-y-4">
      <p class="text-body-lg text-textSecondary">
        Hello! I'm Farhan, a full-stack software engineer based in Pakistan with a passion for 
        building exceptional digital experiences. I specialize in creating scalable web applications, 
        AI-powered solutions, and cloud infrastructure that solve real-world problems.
      </p>
      <p class="text-body-lg text-textSecondary">
        Here's a few technologies I've been working with recently:
      </p>
      <ul class="grid grid-cols-2 gap-2 text-sm text-textSecondary font-mono">
        <li class="flex items-center gap-2">
          <ChevronRight size={14} class="text-accent-indigo" />
          <span>React & Next.js</span>
        </li>
        <li class="flex items-center gap-2">
          <ChevronRight size={14} class="text-accent-indigo" />
          <span>Python & FastAPI</span>
        </li>
        <li class="flex items-center gap-2">
          <ChevronRight size={14} class="text-accent-indigo" />
          <span>TypeScript</span>
        </li>
        <li class="flex items-center gap-2">
          <ChevronRight size={14} class="text-accent-indigo" />
          <span>AWS & Docker</span>
        </li>
        <li class="flex items-center gap-2">
          <ChevronRight size={14} class="text-accent-indigo" />
          <span>PostgreSQL</span>
        </li>
        <li class="flex items-center gap-2">
          <ChevronRight size={14} class="text-accent-indigo" />
          <span>Computer Vision</span>
        </li>
      </ul>
    </div>

    <!-- Quick Facts -->
    <div class="pt-6 border-t border-gray-200">
      <h4 class="text-lg font-bold mb-4">Quick Facts</h4>
      <ul class="space-y-2">
        {quickFacts.map((fact) => (
          <li class="flex items-center gap-2 text-sm text-textSecondary">
            <span class="text-accent-emerald">→</span>
            <span>{fact}</span>
          </li>
        ))}
      </ul>
    </div>
  </div>

  <!-- Right: Image/Video -->
  <div class="relative">
    <div class="glass-card p-4 rounded-lg hover-lift transition-all">
      <div class="relative rounded-lg overflow-hidden">
        <img
          src={poster.src}
          alt="Farhan - Full-Stack Developer"
          class="w-full h-auto rounded-lg"
        />
        <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"></div>
      </div>
    </div>
  </div>
</div>
