---
// props: number (e.g., "10+"), label (e.g., "Complete Work")
const { number, label } = Astro.props;
---

<div class="flex flex-col gap-[8px]">
  <h5 class="font-semibold text-[18px]">{number}</h5>
  <span class="text-[14px] font-normal">{label}</span>
</div>
